<li id="organization-<%= organization.id %>">
	<div class="overlay"><%= organization.name %></div>
	<%= link_to image_tag(organization.logo, :size => "150x150", class: 'imgover'), organization, data: { largesrc: organization.logo, title: organization.name, description: organization.description } %>
</li>