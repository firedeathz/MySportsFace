<% @event.creator.each do |participant| %>
	<div class="part creator-part">
		<%= image_tag("event_creator_msf.png", :width => "30px", :height => "30px") %>
		<%= link_to participant.name, participant, class: "admin add-transition" %><span class="title-part"> (Creator)</span>
	</div>
<% end %>