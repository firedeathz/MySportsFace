<%= provide(:title, @article.title) %>
<div class="back-btn"><%= link_to "Go back", organization_articles_path(@organization), class: "btn btn-lg btn-primary" %></div>
<h1><%= @article.title %></h1>
<h2 class="author">by <%= @article.author %></h2>

<p class="article-description"><%= @article.description %></p>

<div class="comment-form">
	<%= render 'shared/comment_form' %>

	<% @comments.reverse_each do |comment| %>
		<% if comment.user_id != nil %>
			<div class="news-comment">
				<%= gravatar_for User.find(comment.user_id) %>
				<div class="comment-description">
					<p class="comment-author"><%= link_to comment.author, user_path(User.find(comment.user_id)) %></p>
					<span class="comment-timestamp">
						<%= time_ago_in_words(comment.created_at) %> ago.
						<% if current_user.id == comment.user_id %>
							<%= link_to "delete", comment, method: :delete,
														   data: { confirm: "Are you sure you want to delete this comment?" } %>
						<% end %>
					</span>
					<p class="comment-body"><%= comment.body %></p>	
				</div>
			</div>
		<% end %>
	<% end %>
</div>